{
  "name": "zefix-mcp-unofficial",
  "version": "0.1.3",
  "description": "Company search in Swiss Central Business Name Index (zefix.ch)",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "zefix-mcp-unofficial": "dist/index.js"
  },
  "files": [
    "dist",
    "dict"
  ],
  "engines": {
    "node": ">=24.0.0"
  },
  "keywords": ["mcp", "zefix", "switzerland", "company-search", "modelcontextprotocol", "swiss", "business-registry", "search"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.2"
  }
}
